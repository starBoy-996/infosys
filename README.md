## AI Speech Processing Pipeline

### Features
- Text to phoneme conversion
- Phoneme to audio synthesis
- Audio to embedding extraction

### Run
```bash
python -m src.main_pipeline --word she


No README = project looks unfinished. Period.

---

## 5️⃣ Push to GitHub (yes, now)

From project root:

```powershell
git init
git add .
git commit -m "Initial speech pipeline"
git branch -M main
git remote add origin <your-repo-url>
git push -u origin main
# infosys
